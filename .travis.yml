language: java
jdk:
  - oraclejdk8
  - oraclejdk7
  - openjdk7
  - openjdk6
  before_install:
 - chmod +x gradlew
 - chmod +x gradle/wrapper/gradle-wrapper.jar
#install: ls -la;  ls -la gradle/wrapper/gradle-wrapper.*; ./gradlew build
#install: mvn install -Dgpg.skip=true -DskipTests=true -Dmaven.javadoc.skip=true -B -V
#script: mvn test -B

#after_success:
#  - mvn clean cobertura:cobertura coveralls:cobertura
